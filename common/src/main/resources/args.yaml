# 参数配置

# mysql
MYSQL-URL: localhost
MYSQL-PORT: 53306
MYSQL-USERNAME: root
MYSQL-PASSWORD: root

# nacos
NACOS-URL: localhost
NACOS-PORT: 58848

redis:
  host: localhost
  port: 56379
  password: 123456

minio:
  endpoint: http://localhost:59000
  access-key: minioadmin  # 访问密钥（默认 minioadmin）
  secret-key: minioadmin  # 密钥

thread:
  coreSize: 10
  maxSize: 10
  keepAliveTime: 10

wx:
  open:
    app_id: 1
    app_secret: 1
    redirect_url: http://localhost:8080/wx/open/callback
