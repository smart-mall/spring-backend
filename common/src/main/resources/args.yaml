# 参数配置

# mysql
MYSQL-URL: localhost
MYSQL-PORT: 53306
MYSQL-USERNAME: root
MYSQL-PASSWORD: root

# nacos
NACOS-URL: localhost
NACOS-PORT: 58848

minio:
  endpoint: http://localhost:59000
  access-key: minioadmin  # 访问密钥（默认 minioadmin）
  secret-key: minioadmin  # 密钥（